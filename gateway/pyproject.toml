[project]
name = "fde-interview-gateway"
version = "0.1.0"
description = "Local gateway for the FDE interview assistant MVP"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110.0",
  "python-multipart>=0.0.9",
  "httpx>=0.27.0",
  "uvicorn[standard]>=0.29.0",
  "pydantic>=2.7.0",
  "pyyaml>=6.0.0",
]

[tool.uv]
package = false
